{{- if .Values.dex.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-env-config" .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
  AWX_HOSTNAME: {{ .Values.awx.host | quote }}
{{- end }}
